# openclaw
Modded the original "_bash script to provision Openclaw + Kimi 2.5 + Tailscale on fresh Ubuntu box_" by https://github.com/mortalezz

```markdown
# 🚀 OpenClaw Auto-Installer (z.ai + OpenRouter Edition)

Этот репозиторий содержит продвинутый bash-скрипт (`openclaw-setup.sh`) для полностью автоматического развертывания AI-шлюза **OpenClaw**. 

Скрипт **специально оптимизирован для минимальных образов Hetzner VDS** (Ubuntu/Debian) и настроен на использование **z.ai** в качестве основного AI-интеллекта с бесшовным переключением на **OpenRouter** в случае сбоев.

## ✨ Ключевые особенности

- 🧠 **Основной провайдер:** `z.ai` с приоритетной моделью `glm4.7`.
- 🛡 **Отказоустойчивость (Fallback):** Автоматическое переключение на `OpenRouter` (например, Gemini или Claude), если основной провайдер недоступен.
- 🔧 **Hetzner-Оптимизация:** Скрипт элегантно обходит известную проблему зависания `apt upgrade` при обновлении ядра Linux на дешевых VDS.
- 🔐 **Безопасность из коробки:** Изоляция процесса (запуск не от `root`), настройка брандмауэра `ufw`.
- ⚙️ **Работа 24/7:** Автоматическое создание и настройка службы `systemd` для фоновой работы и авто-перезапуска.

---

## ⚡ Быстрый старт (Установка)

Для установки вам потребуется чистый сервер (VDS/VPS) с установленной ОС Ubuntu или Debian и доступ по SSH с правами `root`.

**1. Подключитесь к серверу по SSH:**
```bash
ssh root@ip_вашего_сервера
```

**2. Задайте ваши API-ключи:**
Перед запуском скрипта укажите ваши ключи в переменных окружения:
```bash
export ZAI_API_KEY="ваш_api_ключ_от_zai"
export OPENROUTER_API_KEY="sk-or-ваш_api_ключ_от_openrouter"
```

**3. Скачайте и запустите скрипт:**
*(Замените URL на ссылку вашего репозитория, если вы форкнули проект)*
```bash
curl -O https://raw.githubusercontent.com/ВАШ_ПОЛЬЗОВАТЕЛЬ/openclaw/main/openclaw-setup.sh
chmod +x openclaw-setup.sh
./openclaw-setup.sh
```

☕ Установка занимает пару минут. Скрипт может автоматически перезагрузить сервер (если обновилось ядро ОС) — в этом случае просто подождите, установка продолжится сама!

---

## 🛠 Под капотом: Что, как и почему делает скрипт?

Это не просто скрипт скачивания файлов, а полноценный *provisioning-сценарий*, решающий ряд критических проблем настройки Linux-серверов. Установка разбита на 6 логических фаз:

### Фаза 1: Безопасное обновление ОС (Hetzner Kernel Fix)
* **Проблема:** При обновлении `apt upgrade` на свежих образах Hetzner часто обновляется ядро (kernel). Если не перезагрузить сервер сразу, установщик пакетов намертво зависнет.
* **Решение:** Скрипт проверяет флаг `/var/run/reboot-required`. Если он есть, скрипт прописывает себя в `cron` (`@reboot`), отправляет сервер в ребут и после включения **автоматически продолжает работу** с того же места.

### Фаза 2: Установка зависимостей и фикс `dbus`
* **Проблема:** На минимальных (minimal) образах Ubuntu вырезан системный пакет `dbus-user-session`. Без него невозможно использовать процессы `systemd` от имени обычного пользователя.
* **Решение:** Устанавливаются базовые утилиты (`git`, `curl`, `nodejs`, `ufw`) и принудительно возвращается `dbus-user-session`.

### Фаза 3: Изоляция и безопасность (Создание пользователя)
* **Проблема:** Запускать серверные приложения, смотрящие в интернет, от лица `root` — огромная дыра в безопасности.
* **Решение:** 
  1. Создается отдельный системный пользователь `openclaw`.
  2. Включается `loginctl enable-linger` (позволяет фоновым процессам этого пользователя работать вечно, даже когда SSH-сессия закрыта).
  3. Ваши SSH-ключи копируются новому пользователю для удобного доступа.

### Фаза 4: Настройка Firewall (UFW)
* **Решение:** Скрипт включает брандмауэр и блокирует весь входящий мусорный трафик, оставляя открытыми только два необходимых порта:
  * `22` — для управления по SSH.
  * `18789` — порт по умолчанию, на котором работает сам шлюз OpenClaw.

### Фаза 5: Конфигурация AI-Провайдеров
* **Решение:** Клонируется исходный код OpenClaw, устанавливаются пакеты NPM и генерируется файл `.env`.
* **Почему именно такие настройки:**
  * Скрипт жестко прописывает `PRIMARY_PROVIDER=z.ai` и `PRIMARY_MODEL=glm4.7`. Все запросы летят туда.
  * Настраивается `FALLBACK_PROVIDER=openrouter`. Если API z.ai перегружено или недоступно, OpenClaw автоматически перехватит ошибку и прозрачно отправит запрос в OpenRouter (например, в модели Gemini или Kimi), гарантируя, что ваша работа не прервется.

### Фаза 6: Создание фонового процесса (Systemd)
* **Решение:** Создается системная служба `/etc/systemd/system/openclaw.service`.
* **Почему это нужно:** Это гарантирует, что OpenClaw будет работать как полноценный фоновый процесс. Если приложение случайно упадет (краш кода или нехватка памяти), `systemd` автоматически его перезапустит. При перезагрузке сервера (например, по питанию) шлюз запустится сам.

---

## 🔎 Полезные команды (Шпаргалка)

После успешной установки всё работает в автоматическом режиме. Если вам нужно управлять шлюзом, используйте следующие команды от имени `root`:

**Проверить статус работы (работает ли шлюз):**
```bash
systemctl status openclaw
```

**Посмотреть логи в реальном времени (кто, куда и какие запросы шлет):**
```bash
journalctl -u openclaw -f
```

**Перезапустить шлюз (например, если вы вручную поменяли ключи в `.env`):**
```bash
systemctl restart openclaw
```

**Остановить шлюз:**
```bash
systemctl stop openclaw
```
```
